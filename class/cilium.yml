parameters:
  kapitan:
    compile:
      - input_paths:
          - cilium/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - cilium/component/main.jsonnet
        input_type: jsonnet
        output_path: cilium/
