apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: cilium
  name: hubble-peer
  namespace: cilium
spec:
  internalTrafficPolicy: Local
  ports:
    - name: peer-service
      port: 80
      protocol: TCP
      targetPort: 4244
  selector:
    k8s-app: cilium
