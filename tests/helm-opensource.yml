applications:
  - prometheus

parameters:
  kapitan:
    dependencies:
      - type: https
        source: https://raw.githubusercontent.com/projectsyn/component-prometheus/refs/heads/master/lib/prometheus.libsonnet
        output_path: vendor/lib/prometheus.libsonnet

  cilium:
    install_method: helm
    release: opensource
